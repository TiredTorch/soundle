export enum AppRoutes {
    LOGIN = "/login",
    REGISTER = "/register",
    PROFILE = "/profile",
    FRIENDS = "/friends",
    LIBRARY = "/lib",
    WORKSHOP = "/workshop",
    SETTING = "/setting",
    DASHBOARD = "/dashboard",
}